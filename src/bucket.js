/*
min,max
 */
// function BucketSort (A) {
// 	var b_min = Math.floor(MIN),b_max=Math.ceil(MAX),interval = INTERVAL;
// 	var B=[],B.length = Math.ceil((b_max-b_min)/interval);
// 	for (var i = A.length - 1; i >= 0; i--) {
// 		var index = Math.floor((A[i]-b_min)/interval)
// 		if (B[index] instanceof Array) {
// 			B[index].push(A[i])
// 		} else {
// 			B[index]=[A[i]]
// 		}
// 	}
// 	while(B)
// }